services:
  nginx:
    image: nginx:stable
    environment:
      - ROOT_PATH=${ROOT_PATH}
    ports:
      - "80:80"
    volumes:
      - ./nginx/html:/usr/share/nginx/html:ro
      - ./nginx/conf/nginx.conf.template:/etc/nginx/templates/nginx.conf.template:ro
      - ./nginx/conf/conf.d/json_format.conf:/etc/nginx/json_format.conf:ro
      - /dev/null:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - todo

  todo:
    build: .
    environment:
      - ROOT_PATH=${ROOT_PATH}
      - TODO_DATA_FILE=${TODO_DATA_FILE}
    volumes:
      - ./src:/todo/src:ro
      - ./data:/todo/data

  dozzle:
    image: amir20/dozzle:latest
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro # read docker log
    ports:
      - "8888:8080"
    environment:
      - DOZZLE_LEVEL=info
